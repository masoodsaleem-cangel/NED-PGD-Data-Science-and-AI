START
  │
  ▼
if __name__ == "__main__" → main()
  │
  ▼
LOAD DATA (patients.csv, vitals.csv)
  │
  ▼
DISPLAY MENU (1-10 options)
  │
  ▼
USER CHOICE
  │
  ├─── 1 → ADD PATIENT → Save to list → (Back to MENU)
  │
  ├─── 2 → VIEW ALL PATIENTS → Display → (Back to MENU)
  │
  ├─── 3 → SEARCH PATIENT → Search by ID/Name → Show details + vitals history → (Back to MENU)
  │
  ├─── 4 → MODIFY PATIENT → Edit patient info → Save → (Back to MENU)
  │
  ├─── 5 → DELETE PATIENT → Remove patient + their vitals → (Back to MENU)
  │
  ├─── 6 → ADD VITALS → Select patient → Enter BP, temp, etc. → Save → (Back to MENU)
  │
  ├─── 7 → VIEW VITALS HISTORY → Select patient → Show records → (Back to MENU)
  │
  ├─── 8 → MODIFY VITALS → Edit existing vitals record → Save → (Back to MENU)
  │
  ├─── 9 → DELETE VITALS → Remove vitals record → (Back to MENU)
  │
  ├─── 10 → EXIT → SAVE DATA to CSV → END
  │
  └─── Invalid → Show error → (Back to MENU)